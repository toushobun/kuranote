name: Temporary review export

on:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/tmp-export-review.yml

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/upload-artifact@v4
        with:
          name: review-source
          path: .
          include-hidden-files: true
          retention-days: 1
